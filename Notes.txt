TODO:
Bulk add to queue
save player state (timeline, current item, current duration, all the queues, etc)
reorder item positions in playlists
centralize all the bottomsheet/dialog popups in each major screen
Log statements and toasts/snackbars
Loading popup for things that take time (ex: uploading media)
default "all" playlist (newly uploaded songs automatically added to it - maybe unremovable) 
default sort order setting
keep screen on in expanded player setting
default playlist play mode setting (shuffle vs ordered)
change db + table names to vars in a DatabaseVars file
when copying/storing images, maybe try to make sure there are no duplicated or orphaned images to save space on phone
try to combine editing image + creator at the same time again for media items now that images are done
thoroughly comment code and just make it look better



















To insert metadata:
PS C:\WINDOWS\system32> cd c:/users/gabe/downloads
PS C:\users\gabe\downloads> ffmpeg -i broken_clocks_test_with_ffmpeg.ogg -metadata artist="Amos Roddy" -c copy broken_clocks_new.ogg

For multiple at once:
Inside C:\users\gabe\downloads directory:
PowerShell
Get-ChildItem *.ogg | ForEach-Object { 
    ffmpeg -i $_.Name -metadata artist="Kumi Tanioka" -c copy ("fixed_" + $_.Name) 
}


For Title Case file name and title metadata:
$TextInfo = (Get-Culture).TextInfo
Get-ChildItem *.ogg | ForEach-Object {
    $CleanName = $_.BaseName -replace '_', ' '
    
    $FinalName = $TextInfo.ToTitleCase($CleanName)
    
    ffmpeg -i $_.Name -metadata title="$FinalName" -c copy "$FinalName.ogg"
}

